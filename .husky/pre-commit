unset GIT_DIR GIT_WORK_TREE GIT_INDEX_FILE
bash "$(dirname "$0")/check-branch.sh"
bash "$(dirname "$0")/check-commit-size.sh"
npm run lint
npm run typecheck
npm run coverage
npm run type-coverage
npm run build
git rev-parse -q --verify MERGE_HEAD >/dev/null || node dist/cli.js .veto/ --staged
